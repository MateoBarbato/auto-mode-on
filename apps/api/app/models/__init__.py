"""Pydantic schemas — the strict-JSON contracts (LLM output ↔ DB insert).

⚠️ P4 owns this package (see roles-and-ownership.md). These are placeholder
stubs so the routers import cleanly; P4 defines the real extraction/output
schemas from `prompts/*.md`. Coordinate before adding models here.
"""
